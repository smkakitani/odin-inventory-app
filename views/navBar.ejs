<nav>
  <menu>
    <% for (let i = 0; i < links.length; i++) { %>
        <li>
          <a href="<%= links[i].href %>">
            <span> <%= links[i].title %> </span>
          </a>
        </li>
    <% } %>
  </menu>
</nav>